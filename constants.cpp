#include <iostream>

int main(){
    const double pi = 3.14; //now, this is a constant throughout the code
    //pi=0; so that we can't assume values for "pi" like this
    std::cout << pi;
    return 0;
}